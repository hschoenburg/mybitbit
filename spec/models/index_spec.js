// TEST ALL THE MODELS

//var factory = require('../support/factory')

describe('User', function() {

  it('should have a first_name prop', function(done) {
    //factory.build('user').then(function(user) {

    //expect(user.first_name).toNot.BeNull()
      done();
  })
})
